<div class="container-fluid">
  <div class="hero-unit">
  <h1>Task: Correr XAPIEN</h1>
  <!--
  <ul style="margin: 20px 0 0 40px">
    <li>Elija el archivo con extensión ".XLS" o ".XLSX"</li>
    <li>Seleccione el poder al cual pertenece El archivo a subir</li>
    <li><small>Oprima el botón "Subir datos"</small></li>
    <li><small>Esta operación puede tardar algunos minútos, por favor aguarde a que termine el proceso</small></li>
  </ul>
  -->
  </div>
  <div class="row-fluid">
    <div class="span4"></div>
    <div class="span4" style="">
		<ul>
		<%
		env = Rails.env
		%>
			<li><h2>Enviroment = <%= env %></h2></li>

		<% if env == "production" || env == "pre" %>
      <li>XAPIEN = <pre><%= `/usr/local/lib/ruby/gems/1.9.1/gems/rake-10.1.0/bin/rake  ddjj:load_index 2>&1` %></pre></li>
    <% else %>
      <li>XAPIEN = <pre><%= # `rake ddjj:remove_index` 
      %></pre></li>
      <li>XAPIEN = <pre><%=  `rake ddjj:load_index 2>&1` 
      %></pre></li>
		<% end %>
			<li>$: <%= $?
      %></li>

		</ul>
    </div>
    </div>
    <div class="span4"></div>
  </div>
</div>
<div id="loading"></div>
